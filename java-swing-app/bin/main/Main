package main;

import view.LoginSelectionFrame;

public class main {
    public static void Main(String[] args) {
        // Nyalain UI di Event Dispatch Thread (best practice Swing)
        javax.swing.SwingUtilities.invokeLater(() -> {
            new LoginSelectionFrame();
        });
    }
}
